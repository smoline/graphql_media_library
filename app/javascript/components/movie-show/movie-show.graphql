query MovieShowQuery($id: ID!) {
  movie(id: $id) {
    id
    title
    description
    releaseDate
    movieImageUrl
    runtime
    tagline
    tmdbId
    imdbId
    casts {
      id
      character
      person {
        id
        name
        birthday
      }
    }
  }
}